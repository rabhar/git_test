pagination: {
    xpathHighlight: null,
    loadui: function() {
      var that = this;
      var notes =
        "<note>Please ensure that all product titles or all product images are highlighted, As it will be recorded as product xpath</note>";

      if (storageCache.website.pe.xpathArray.length) {
        Ui.loadUi(
          "minion_select.png",
          "Are all the pagination links highlighted ? ",
          "yes",
          "no",
          "Toggle highlighted urls",
          notes,
          function() {
            that.xpathHighlighter = new XpathHighlighter(
              storageCache.website.pe.xpathArray
            );
            $("#common_container_ext").hide();
            that.secondButton();
          }
        );
      }
    }
}